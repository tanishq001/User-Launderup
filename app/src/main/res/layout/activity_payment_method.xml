<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.view.activities.PaymentMethodActivity">

    <ImageView
        android:id="@+id/payment_method_top"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:src="@drawable/page_top"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/payment_method_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/select_payment_method"
        app:layout_constraintTop_toBottomOf="@+id/payment_method_top"
        android:textSize="24sp"
        android:padding="20dp"
        app:layout_constraintStart_toStartOf="parent"
        android:textColor="#000000"/>

    <RadioGroup
        android:id="@+id/paymentRadioGroup"
        android:checkedButton="@+id/radio_button_1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/payment_method_text"
        android:layout_marginTop="20sp"
        android:layout_marginStart="20sp">

        <RadioButton
            android:id="@+id/paytm_radio_button"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="Paytm"
            android:buttonTint="#006FB9"
            android:textSize="20sp"/>

        <RadioButton
            android:id="@+id/razorpay_radio_button"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="Razorpay"
            android:buttonTint="#006FB9"
            android:textSize="20sp"/>

    </RadioGroup>

    <Button
        android:id="@+id/continue_payment_button"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="@string/Continue"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginBottom="20dp"
        android:backgroundTint="#23A4FF"/>

</androidx.constraintlayout.widget.ConstraintLayout>